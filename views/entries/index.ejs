<%- include('../partials/header') %>

<h2>My Trips</h2>
    <p>Locations:</p>
  <tbody>
    <% entries.forEach(function(e) { %>
      <tr>
        <!-- <td><%= e.location %></td> -->
        <td><a href="/entries/<%= e._id %>"> <%= e.location %></a></td>
      </tr>
    <% }); %>
  </tbody>
</table>

<%- include('../partials/footer') %>